
var lacoPipe = function() {
    var self = {
        basePath = '',
        handlers: {}
    };

    self.init = function() {
        if(!window.jQuery) return console.error('Jquery must be loaded before LacoPipe is initializing!')
        self.listener();
    }
}