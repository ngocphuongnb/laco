var LacoMedia = function() {
    var self = {
        fileChooserSelectors: ['.image-field-thumb', '.laco-choose-file']
    };

    self.init = function() {
        new LacoModal($(self.fileChooserSelectors.join(',')));
    }
    return self;
};