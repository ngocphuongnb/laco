var lacoMedia = function() {
    var self = {
        pickFileSelectors: ['.image-field-thumb', '.laco-pick-file']
    };

    self.init = function() {
        $(document).on('click', self.pickFileSelectors.joinoin(' '))
    }
};