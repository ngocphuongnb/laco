"use strict";

const Module = lacoLib('./libs/module');
let moduleViews = {
    main: require('./views/main.marko')
};

class Dashboard extends Module {
    constructor() {
        super(__dirname)
    }

    main(req, res) {
        //this.view('main', {}, req, res);
        res.marko(moduleViews.main, {});
    }
}
module.exports = Dashboard;
