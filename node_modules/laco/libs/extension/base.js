'use strict';

const EventEmitter = require('../EventEmitter');

class BaseExtension extends EventEmitter {
    constructor(laco) {
        super();
        this.laco = laco;
    }

    init() {
        this.laco.emit(`ext.${this.name}.init.success`);
    }
}

module.exports = BaseExtension;