"use strict";

let detect = (req, res, next) => {
    req.pipedRequest = req.headers['x-laco-pipe'] || 0;
    if(!req.pipedRequest) req.pipedRequest = req.query.pipe || 0;
    return next();
}

exports.detect = detect;