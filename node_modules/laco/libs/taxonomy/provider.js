"use strict";
const taxonomyModel = require('./model');

let get = data => {
    return taxonomyModel.find(data);
}

let withIds = ids => {
    if(!_.isArray(ids)) ids = [ids];
    ids = _.map(ids, id => 1*id);
    let findMethod = ids[1] ? 'find' : 'findOne';
    return taxonomyModel[findMethod]({intId: id}).exec();
}

exports.model = taxonomyModel;
exports.get = get;
exports.withIds = withIds;