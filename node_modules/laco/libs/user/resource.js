"use strict";

module.exports = class Resource {
    constructor(userData) {
        for(let attr in userData)
            this[attr] = userData[attr];
        if(!this.roles) this.roles = [];
    }
    can(...args) {
        return ( this.roles.all || (args[0] && this.roles[args[0]]) );
    }
}