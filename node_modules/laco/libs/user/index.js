"use strict";

const g = lacoLib('./libs/global');
const userModel = require('./model');

let init = () => {
    userModel.register();
}

let add = (username, email, password) => {
    let _user = new g.models.user({username, email, password});
    return _user.save()
}

exports.init = init;
exports.add = add;