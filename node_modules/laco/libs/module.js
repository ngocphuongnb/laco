"use strict";

const path = require('path');

class Module {
    constructor(moduleDir) {
        this.moduleDir = moduleDir;
        this.viewsObj = {}
    }
    setViews(viewsObj) {
        this.viewsObj = viewsObj;
    }
    view(viewName, data, req, res) {
        res.end('OKK: ' + viewName);
    }
}

module.exports = Module;