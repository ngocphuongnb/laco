'use strict';

const Command = lacoLib('libs/commander/base');

class UICommand extends Command {
    constructor(subAppManager) {
        super();
        this.subAppManager = subAppManager;

    }

    exec() {
        console.log('exec');
    }

    list(registeredApps, argv) {
        console.log(registeredApps);
    }
}

module.exports = UICommand;