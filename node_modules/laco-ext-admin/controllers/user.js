'use strict';

const BaseController = lacoLib('libs/controller/base');

class User extends BaseController {
    constructor(manager, controller) {
        super(manager, controller);
        this.a = 'dc';
    }

    index(req, res, next) {
        res.end('Admin user index')
    }

    login(req, res, next) {
        this.view('user.login@layout_default', { req, res });
    }
}

module.exports = User;