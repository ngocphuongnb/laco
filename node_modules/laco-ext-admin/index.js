'use strict';

const adminRoutes = require('./routes');

class LacoAdmin {
    constructor(laco) {
        this.laco = laco;
        let admin = new this.laco.SubApp('admin');

        admin.router.register(adminRoutes);
        admin.controller.register();
        this.laco.subAppManager.register('/admin', admin);
    }
}

module.exports = LacoAdmin;