'use strict';

const BaseUI = lacoLib('libs/ui/base');

class SemanticUIAdmin extends BaseUI {
    constructor(subapp, ui) {
        super(subapp, ui, {});

        this.discovery(err => {
            console.log('discovery done')
                // this.subapp.laco.emit(err ? 'ui.SemanticUIAdmin.loaded' : 'ui.SemanticUIAdmin.error', err);
            this.subapp.laco.emit('ui.SemanticUIAdmin.loaded');
        });
    }
}

module.exports = SemanticUIAdmin;